[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Environment variables for Railway deployment
# These will be overridden per service in Railway dashboard
[env]
RUST_LOG = "info"
CHAIN = "dev"